<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.holaclimbing.server.domain.user.mapper.UserMapper">

    <sql id="columns">
        id, email, password_hash, email_verified, email_verification_token,
        provider, provider_id, nickname, profile_image, bio,
        last_login_at, created_at, updated_at, deleted_at
    </sql>

    <insert id="insert" parameterType="com.holaclimbing.server.domain.user.domain.User"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (email, password_hash, email_verified, email_verification_token, nickname)
        VALUES (#{email}, #{passwordHash}, #{emailVerified}, #{emailVerificationToken}, #{nickname})
    </insert>

    <select id="findById" resultType="com.holaclimbing.server.domain.user.domain.User">
        SELECT <include refid="columns"/>
        FROM users
        WHERE id = #{id} AND deleted_at IS NULL
    </select>

    <select id="findByEmail" resultType="com.holaclimbing.server.domain.user.domain.User">
        SELECT <include refid="columns"/>
        FROM users
        WHERE email = #{email} AND deleted_at IS NULL
    </select>

    <select id="findByEmailVerificationToken" resultType="com.holaclimbing.server.domain.user.domain.User">
        SELECT <include refid="columns"/>
        FROM users
        WHERE email_verification_token = #{token} AND deleted_at IS NULL
    </select>

    <select id="existsByEmail" resultType="boolean">
        SELECT EXISTS(SELECT 1 FROM users WHERE email = #{email})
    </select>

    <select id="existsByNickname" resultType="boolean">
        SELECT EXISTS(SELECT 1 FROM users WHERE nickname = #{nickname})
    </select>

    <update id="markEmailVerified">
        UPDATE users
        SET email_verified = TRUE,
            email_verification_token = NULL,
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <update id="updateEmailVerificationToken">
        UPDATE users
        SET email_verification_token = #{token},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <update id="updateLastLoginAt">
        UPDATE users
        SET last_login_at = NOW(),
            updated_at = NOW()
        WHERE id = #{id}
    </update>

</mapper>
