<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.holaclimbing.server.domain.video.mapper.CommentMapper">

    <insert id="insert" parameterType="com.holaclimbing.server.domain.video.domain.Comment"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO comments (user_id, video_id, parent_id, content)
        VALUES (#{userId}, #{videoId}, #{parentId}, #{content})
    </insert>

    <select id="findById" resultType="com.holaclimbing.server.domain.video.domain.Comment">
        SELECT comments.id, comments.user_id, comments.video_id, comments.parent_id, comments.content,
               u.nickname,
               u.profile_image,
               comments.created_at, comments.updated_at, comments.deleted_at
        FROM comments
        JOIN users u ON u.id = comments.user_id
        WHERE comments.id = #{id} AND comments.deleted_at IS NULL
    </select>

    <!-- viewer와 댓글 작성자 사이에 차단 관계가 있으면 댓글을 제외하는 공통 조건. -->
    <sql id="notBlockedByViewer">
        <if test="viewerId != null">
            AND NOT EXISTS (
                SELECT 1 FROM user_blocks
                WHERE (blocker_id = #{viewerId} AND blocked_id = comments.user_id)
                   OR (blocker_id = comments.user_id AND blocked_id = #{viewerId})
            )
        </if>
    </sql>

    <select id="findByVideoId" resultType="com.holaclimbing.server.domain.video.domain.Comment">
        SELECT comments.id, comments.user_id, comments.video_id, comments.parent_id, comments.content,
               u.nickname,
               u.profile_image,
               comments.created_at, comments.updated_at, comments.deleted_at
        FROM comments
        JOIN users u ON u.id = comments.user_id
                    AND u.status = 'ACTIVE'
                    AND u.deleted_at IS NULL
        WHERE comments.video_id = #{videoId} AND comments.deleted_at IS NULL
        <include refid="notBlockedByViewer"/>
        ORDER BY comments.created_at, comments.id
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="countByVideoId" resultType="long">
        SELECT COUNT(*)
        FROM comments
        JOIN users u ON u.id = comments.user_id
                    AND u.status = 'ACTIVE'
                    AND u.deleted_at IS NULL
        WHERE comments.video_id = #{videoId} AND comments.deleted_at IS NULL
        <include refid="notBlockedByViewer"/>
    </select>

    <update id="update">
        UPDATE comments SET content = #{content}, updated_at = NOW()
        WHERE id = #{id} AND deleted_at IS NULL
    </update>

    <update id="softDelete">
        UPDATE comments SET deleted_at = NOW(), updated_at = NOW() WHERE id = #{id}
    </update>

</mapper>
