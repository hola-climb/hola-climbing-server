global:
  scrape_interval: 15s
  scrape_timeout: 10s

scrape_configs:
  - job_name: hola-climbing-server
    metrics_path: /actuator/prometheus
    static_configs:
      - targets:
          - host.docker.internal:8080   # 호스트에서 실행 중인 Spring 앱
